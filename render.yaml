services:
  - type: web
    name: talentdash
    env: python
    plan: free

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn dashboard.app:app

    envVars:
      - key: DATABASE_URL
        sync: false

      - key: GEMINI_API_KEY
        sync: false